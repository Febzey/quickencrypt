/**
 * Check if two hashes are the same.
 * @param hash1 
 * @param hash2 
 * @returns boolean
 */
 export function compareHashes(hash1: string, hash2: string): boolean {
    return hash1 === hash2;
}